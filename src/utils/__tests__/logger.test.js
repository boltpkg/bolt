// @flow
import * as logger from '../logger';

describe('logger', () => {
  test('prefix()');
  test('log()');
  test('title()');
  test('info()');
  test('warn()');
  test('error()');
  test('success()');
  test('stdout()');
  test('stderr()');
});
