// @flow
import add from './add';
import exec from './exec';
import remove from './remove';
import run from './run';
import upgrade from './upgrade';

export default {
  add: add,
  exec: exec,
  remove: remove,
  run: run,
  upgrade: upgrade,
};
