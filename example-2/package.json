{
  "private": true,
  "name": "fixture-project",
  "scripts": {
    "clean": "rm -rf packages/**/node_modules && rm -rf node_modules"
  },
  "pworkspaces": [
    "packages/*"
  ],
  "dependencies": {
    "chalk": "^2.1.0",
    "react": "^15.6.1"
  }
}
